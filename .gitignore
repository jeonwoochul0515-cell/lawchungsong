*.log
.DS_Store
node_modules/
.env

# 네이버 API 키 + 로컬 리포트 — 커밋 금지 (공개 저장소 유출 방지)
/scripts/naver-keys.json
/scripts/reports/

# 작업 중 붙여넣은 스크린샷 — 커밋 금지 (API 키 등 민감정보 포함 가능)
/image.png
/image copy*.png
/_verify*.png
.vercel

# 로컬 개발용 환경변수 — 커밋 금지 (실제 값은 Vercel 환경변수가 정본)
.env.local
